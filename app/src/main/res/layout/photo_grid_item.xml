<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingLeft="@dimen/two"
    android:paddingStart="@dimen/two">

    <ImageView
        android:id="@+id/ivPhotoGrid"
        android:layout_width="@dimen/forty_two"
        android:layout_height="@dimen/forty_two"
        android:scaleType="centerCrop"/>

    <com.codepan.widget.CodePanButton
        android:id="@+id/btnPhotoGrid"
        android:layout_width="@dimen/forty_two"
        android:layout_height="@dimen/forty_two"
        android:background="@drawable/state_rect_trans_dark"/>

</FrameLayout>